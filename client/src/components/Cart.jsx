import React, { useState } from 'react';




  return (
  
          
            ))}
          </ul>
          <p>Total: ${totalPrice}</p>
          <button>Proceed to Checkout</button>
        </div>
      )}
    </div>
  );
}

export default Cart;
